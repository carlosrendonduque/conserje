{
    "name": "carlosrendonduque/conserje-backend",
    "description": "Backend for Conserje: holds the LLM credentials, drives the qualification conversation, and dispatches qualified leads to n8n.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.2",
        "ext-json": "*",
        "ext-curl": "*",
        "anthropic-ai/sdk": "^0.50.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "Conserje\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Conserje\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "lint": "find src public tests -name '*.php' -print0 | xargs -0 -n1 php -l"
    },
    "config": {
        "sort-packages": true
    }
}
